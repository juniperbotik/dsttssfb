name: you name

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: update and upgrade
        run: |
          apt-get update -y
          apt-get upgrade -y

      - name: you name
        run: |
          you command
          you command
          you command

      - name: you name
        run: |
          you command
          you command
          you command
